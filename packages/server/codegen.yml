overwrite: true

schema:
  - ./src/graphql/typedefs.ts

documents: null

generates:
  ../types/graphql-types.ts:
    plugins:
      - typescript
      - typescript-resolvers


config:
  federation: true
  namingConvention:
    enumValues: keep
